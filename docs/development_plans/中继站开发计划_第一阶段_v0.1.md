# 《中继站》开发计划 · 第一阶段「基础验证」

> v0.1｜2026-09-06｜对应《中继站策划案 v0.1》6.2 阶段表第一行「基础验证」
> 推进顺序：W1 架构地基 + 格子地图 → W2 角色 + 设施 + 任务 → W3 行为闭环 + UI + 集成
> 任务不指定负责人，由团队按依赖关系认领；每周演示可运行版本（对应策划案 6.3）。

## 1. 目标与范围

**阶段目标**：灰盒格子地图中，一名角色可完成一次修复任务闭环。

**本阶段三个重心**（2026-09-06 确认）：

1. **技术架构与模块接口**：搭建支撑 4 名程序员并行开发的分层架构，冻结接口契约 v1；
2. **格子地图架构**：用格子（Tile）表达圆环形基地，含数据模型、渲染与寻路；
3. **三人角色架构**：角色数据与行为框架按三人设计（需求、专长、任务选择），本阶段先以 1 人跑通闭环。

**阶段交付物**：可运行的灰盒 Demo 场景（验收标准见 §10）+ 冻结的模块接口契约（§8）。

## 2. 技术架构约定

### 2.1 分层原则：模拟层与表现层分离

| 层 | 目录 | 职责 | 约束 |
| --- | --- | --- | --- |
| 模拟层 | `core/` | 游戏规则：时间、地图数据、角色、任务、设施 | **禁止引用 Node / 场景 / PackedScene**；仅允许 Godot 基础值类型（如 `Vector2I`） |
| 表现层 | `game/` | Node 场景、渲染、输入、UI、音画反馈 | 只通过接口与事件读写模拟层，不私藏游戏状态 |

理由：模拟层不依赖 Node 才能做单元测试；三人行为逻辑、任务分配等核心规则可脱离引擎调试；为阶段二资源/氧气模拟和后续存档留出干净内核。

### 2.2 目录结构

```
res://
├── core/                      # 模拟层（禁 Node）
│   ├── Common/                # 值对象、通用工具
│   ├── Events/                # IGameEvent 定义 + IEventBus
│   ├── Time/                  # 游戏时钟（倍率/暂停/累计分钟）
│   ├── Map/                   # 格子数据模型、功能区、寻路封装
│   ├── Characters/            # 角色数据、需求框架、行为状态
│   ├── Tasks/                 # 任务生命周期、任务板
│   └── Facilities/            # 设施状态机
├── game/                      # 表现层
│   ├── Autoloads/             # GameRoot.cs（组合根，唯一 Autoload）
│   ├── Main/                  # Main.tscn 主场景
│   ├── Map/                   # MapView（TileMapLayer 渲染、hover 高亮）
│   ├── Characters/            # CharacterAgent（Node2D，移动与头顶标签）
│   ├── Facilities/            # FacilityView（状态着色/图标）
│   ├── Input/                 # 点击拾取、选中逻辑
│   └── UI/                    # 时间条、设施面板等 Control
├── resources/                 # .tres 数据定义（角色×3、设施、地图布局）
├── tests/                     # core 层单元测试（xUnit）
└── docs/
```

命名空间：`RelayStation.Core.*` / `RelayStation.Game.*`（程序集名保持 `Online_AI_Town` 不改，命名空间仅内部使用，2026-09-06 确认）。

### 2.3 全局服务与 Autoload

- **只注册一个 Autoload：`GameRoot`**（`game/Autoloads/GameRoot.cs`），作为组合根构建并持有模拟层各服务；
- `GameRoot._Process` 按时间倍率驱动 `Simulation.Tick()`；
- 表现层节点通过 `GameRoot.Instance.Simulation` 获取服务，或订阅事件总线；
- 明确禁止：在其他 Node 里用静态可变状态当全局变量。

### 2.4 数据驱动

用 Godot `Resource`（.tres）承载可配置数据，策划后续调数值不改代码：

- `CharacterDef`：id、姓名、出身、专长（影响效率的作业类型）、特质占位——**建满 3 份**；
- `FacilityDef`：id、名称、占地格、修复耗时（游戏分钟）、初始状态；
- 地图布局：格子数据 + 功能区划分（W1 内定稿为 .tres 或代码常量，原型从简）。

### 2.5 协作与工程约定

- 引擎版本锁定：Godot 4.7.2 stable (mono)，.NET SDK 8；
- 分支：`main` 受保护；`feature/<模块>-<任务号>` 分支开发，PR 至少一人 review（任务跨模块时由主程 review）；
- PR 检查清单包含：`core/` 无 Node 引用、接口变更同步更新本计划 §8；
- 代码风格：`.editorconfig` 已有，缩进与命名以现有配置为准；
- 代码注释：所有接口/类/方法/属性/事件/枚举须使用统一注释头（`Date` / `Name` / `Description`，中文优先，可用英文），格式与示例见 §8；注释只在实现处（优先）或声明处编写，不在调用处编写；
- 每周集成演示可运行版本（先保证 `main` 可运行，再演示功能）。

## 3. 地图架构（格子地图）

### 3.1 圆环基地的格子化方案（W1 初定稿）

| 方案 | 做法 | 优点 | 风险 |
| --- | --- | --- | --- |
| **A：环形格子近似（倾向）** | 方形网格上按「到中心距离 ∈ [内径, 外径]」生成环状走廊，5 个功能区沿环分布为局部加宽舱室 | 俯视直观符合策划案「圆环形基地」；TileMap/AStarGrid2D 原生支持 | 圆环用方格拼出有锯齿感，需灰盒验证观感 |
| **B：环展开条带** | 圆环切成段铺成横向长条走廊（类 FTL 舱段） | 实现最简单、格子利用率高 | 与「圆环」表现不符 |

**决策规则**：W1 先做 A/B 灰盒对比，以观感 + 实现成本定稿；默认 A，观感不佳降级 B。参考尺寸：方案 A 约 48×48 格、环外径约 20 格、走廊宽 3 格、Tile 32×32。

### 3.2 格子数据模型

- `CellKind`：`Vacuum`（不可通行）/ `Floor` / `Wall` / `Door` / `FacilitySlot`；
- `ZoneId`：`Living` / `Power` / `LifeSupport` / `AirlockStorage` / `Communication` 五个功能区，灰盒用底色区分；
- `IGridMap` 提供：格子查询、可通行判定、功能区归属查询；
- 坐标换算统一走 `MapView.LocalToMap / MapToLocal`，模拟层只认 `Vector2I` 格子坐标。

### 3.3 寻路

- 封装 Godot `AStarGrid2D`（非 Node 类型，允许在 `core/` 使用）实现 `IPathfinding`；
- 开发期提供路径调试可视化（`game/` 内画线），验收后可隐藏。

### 3.4 渲染

- 使用 `TileMapLayer`（Godot 4.3+，勿用已废弃的 `TileMap` 节点）；
- 灰盒阶段：地板/墙体用纯色 Tile，功能区以底色区分，设施占位用色块 + 文字。

## 4. 角色架构（面向三人的设计）

### 4.1 三层数据模型

| 层 | 类型 | 说明 |
| --- | --- | --- |
| 定义 | `CharacterDef`（Resource，建满 3 份） | 维修人员 / 系统操作员 / 外勤作业员：出身、专长、特质占位 |
| 模拟 | `CharacterSim`（core） | 当前状态、所在格、当前任务、需求快照、效率倍率 |
| 表现 | `CharacterAgent`（Node2D） | 沿路径移动、头顶任务标签；只读 `CharacterSim` 并订阅变更事件 |

**角色数量由配置驱动**：演示场景默认 1 人，改资源数组即可 3 人同屏——这是「三人架构」在本阶段的落地方式。

### 4.2 需求框架（占位）

- `INeedSystem` 按策划案定义呼吸 / 进食 / 休息三项需求（0~100），本阶段仅按时间缓慢衰减、**不致命、不生效死逻辑**；
- 目的：冻结需求数据结构与更新接口，阶段二接入真实生存压力时不动架构。

### 4.3 行为状态机

`Idle → Moving → Working → (Done | Interrupted) → Idle`

- 阶段一行为简化为「玩家下达修复任务 → 角色认领 → 寻路前往 → 执行 → 完成」；
- 任务选择接口 `TaskBoard.PickFor` 的签名按「需求 + 优先级 + 可用性」预留，阶段一实现为「玩家显式指派」，阶段二接入工作优先级面板；
- 专长通过 `WorkSpeedMultiplier` 影响修复耗时（接口本阶段接通，数值从简）。

## 5. 任务与设施系统（最小闭环）

### 5.1 任务生命周期

`Pending → Assigned → InProgress → Done / Cancelled`

- 本阶段唯一任务类型：`RepairTask`（目标设施 + 修复耗时）；
- 玩家点击损坏设施 → 面板点「修复」→ 任务进入 `Pending` → 空闲角色认领。

### 5.2 设施状态机

`Damaged → UnderRepair → Operational`，状态变化发事件，`FacilityView` 变色呈现。
本阶段放 1~2 台损坏设施（建议其中一台为制氧机，为阶段二留语义），不接真实运行效果。

### 5.3 Demo 剧本（修复闭环）

1. 运行 `Main.tscn`，显示灰盒环形基地与待机角色；
2. 点击损坏设施 → 弹出面板显示名称 / 状态 / 修复按钮；
3. 点击「修复」→ 角色头顶出现任务标签，自动寻路前往；
4. 到达后进入 Working，进度按游戏分钟推进（受专长效率影响）；
5. 完成 → 设施变色为 Operational，角色回到 Idle，事件日志（DebugOverlay 打印即可）。

## 6. 时间系统

- `IGameClock`：累计游戏分钟、暂停、1×/2×/4× 倍率；
- 换算：正常速度下 **1 现实秒 = 1 游戏分钟**（倍率即 ×60 ×N）；
- `GameMinuteElapsed` 事件供需求衰减等系统订阅；
- 顶部时间条提供暂停/加速按钮与游戏时钟显示。

## 7. 最小 UI

| 组件 | 内容 |
| --- | --- |
| 时间条 | 游戏时钟、暂停/1×/2×/4× 按钮 |
| 设施面板 | 点击设施显示：名称、状态、修复按钮 |
| 头顶标签 | 角色当前任务文字（阶段一用 Label，不做气泡美术） |
| DebugOverlay | 鼠标格子坐标、路径可视化、事件日志打印（仅开发期） |

## 8. 模块接口契约（C# 草案，W3 评审后冻结 v1）

> 以下为签名级草案，实现细节由开发者填充；任何接口变更需同步修改本节并走 PR。

**注释规范（后续开发必须遵守）**：所有接口、类、方法、属性、事件及枚举均须附带统一注释头，格式如下（内容用中文优先，可用英文填写）：

```csharp
/*****
Date: 2026-09-06
Name: <成员名>
Description: <功能/职责说明>
*****/
```

其中 `Date` 为编写日期，`Name` 为成员名称，`Description` 为功能/职责说明。

**注释位置约束**：注释只在实现处（优先）或声明处编写，不在调用处编写。

```csharp
namespace RelayStation.Core.Events;

/*****
Date: 2026-09-06
Name: IGameEvent
Description: 游戏事件标记接口；所有事件类型必须实现该接口，以便事件总线统一处理。
*****/
public interface IGameEvent { }

/*****
Date: 2026-09-06
Name: IEventBus
Description: 事件总线接口；提供事件的发布与订阅能力，解耦模拟层与表现层。
*****/
public interface IEventBus
{
    /*****
    Date: 2026-09-06
    Name: Publish
    Description: 发布一个游戏事件，通知所有已订阅该事件类型的处理器。
    *****/
    void Publish<TEvent>(TEvent e) where TEvent : IGameEvent;

    /*****
    Date: 2026-09-06
    Name: Subscribe
    Description: 订阅指定类型的游戏事件；事件发布时回调传入的处理器。
    *****/
    void Subscribe<TEvent>(Action<TEvent> handler) where TEvent : IGameEvent;

    /*****
    Date: 2026-09-06
    Name: Unsubscribe
    Description: 取消订阅指定类型的游戏事件，移除对应的处理器。
    *****/
    void Unsubscribe<TEvent>(Action<TEvent> handler) where TEvent : IGameEvent;
}
```

```csharp
namespace RelayStation.Core.Time;

/*****
Date: 2026-09-06
Name: IGameClock
Description: 游戏时钟接口；管理累计游戏分钟、暂停与倍速，驱动游戏时间的推进。
*****/
public interface IGameClock
{
    /*****
    Date: 2026-09-06
    Name: TotalGameMinutes
    Description: 已累计的游戏总分钟数。
    *****/
    double TotalGameMinutes { get; }

    /*****
    Date: 2026-09-06
    Name: IsPaused
    Description: 是否处于暂停状态。
    *****/
    bool IsPaused { get; }

    /*****
    Date: 2026-09-06
    Name: SpeedMultiplier
    Description: 当前时间倍速；取值范围 1 / 2 / 4。
    *****/
    int SpeedMultiplier { get; }

    /*****
    Date: 2026-09-06
    Name: SetPaused
    Description: 设置暂停状态。
    *****/
    void SetPaused(bool paused);

    /*****
    Date: 2026-09-06
    Name: SetSpeed
    Description: 设置时间倍速。
    *****/
    void SetSpeed(int multiplier);

    /*****
    Date: 2026-09-06
    Name: Advance
    Description: 按现实秒数推进游戏时间（由 GameRoot 驱动调用）。
    *****/
    void Advance(double realSeconds);

    /*****
    Date: 2026-09-06
    Name: GameMinuteElapsed
    Description: 每累计满 1 个游戏分钟时触发的事件；参数为该分钟消耗的现实秒数。
    *****/
    event Action<double>? GameMinuteElapsed;
}
```

```csharp
namespace RelayStation.Core.Map;

/*****
Date: 2026-09-06
Name: CellKind
Description: 格子类型枚举；标识地图上每一格的地形类别。
*****/
public enum CellKind { Vacuum, Floor, Wall, Door, FacilitySlot }

/*****
Date: 2026-09-06
Name: ZoneId
Description: 功能区标识枚举；标识基地的五大功能区域。
*****/
public enum ZoneId { Living, Power, LifeSupport, AirlockStorage, Communication }

/*****
Date: 2026-09-06
Name: IGridMap
Description: 格子地图接口；提供格子查询、可通行判定与功能区归属查询。
*****/
public interface IGridMap
{
    /*****
    Date: 2026-09-06
    Name: Width
    Description: 地图宽度（格子数）。
    *****/
    int Width { get; }

    /*****
    Date: 2026-09-06
    Name: Height
    Description: 地图高度（格子数）。
    *****/
    int Height { get; }

    /*****
    Date: 2026-09-06
    Name: GetCell
    Description: 获取指定格子的地形类型。
    *****/
    CellKind GetCell(Vector2I cell);

    /*****
    Date: 2026-09-06
    Name: IsWalkable
    Description: 判断指定格子是否可通行。
    *****/
    bool IsWalkable(Vector2I cell);

    /*****
    Date: 2026-09-06
    Name: GetZone
    Description: 获取指定格子所属的功能区；不属于任何功能区时返回 null。
    *****/
    ZoneId? GetZone(Vector2I cell);
}

/*****
Date: 2026-09-06
Name: IPathfinding
Description: 寻路接口；基于格子地图计算两点之间的可通行路径。
*****/
public interface IPathfinding
{
    /*****
    Date: 2026-09-06
    Name: FindPath
    Description: 计算从起点到终点的格子路径（含起终点）；不可达时返回空列表。
    *****/
    IReadOnlyList<Vector2I> FindPath(Vector2I from, Vector2I to);
}
```

```csharp
namespace RelayStation.Core.Characters;

/*****
Date: 2026-09-06
Name: CharacterState
Description: 角色行为状态枚举；描述角色当前所处的行为阶段。
*****/
public enum CharacterState { Idle, Moving, Working, Interrupted }

/*****
Date: 2026-09-06
Name: NeedSnapshot
Description: 需求快照；记录角色三项需求的当前数值（0-100）。
*****/
public sealed record NeedSnapshot(float Oxygen, float Food, float Rest);

/*****
Date: 2026-09-06
Name: INeedSystem
Description: 需求系统接口；负责角色的需求数值衰减与读取。
*****/
public interface INeedSystem
{
    /*****
    Date: 2026-09-06
    Name: Update
    Description: 按游戏分钟推进指定角色的需求衰减。
    *****/
    void Update(CharacterSim c, double deltaGameMinutes);

    /*****
    Date: 2026-09-06
    Name: Read
    Description: 读取指定角色当前的需求快照。
    *****/
    NeedSnapshot Read(CharacterSim c);
}

/*****
Date: 2026-09-06
Name: CharacterSim
Description: 角色模拟对象；持有角色的定义、状态、所在格、当前任务与效率倍率等运行时数据。
*****/
public sealed class CharacterSim
{
    /*****
    Date: 2026-09-06
    Name: Def
    Description: 角色的静态定义（出身、专长、特质等）。
    *****/
    public CharacterDef Def { get; init; } = default!;

    /*****
    Date: 2026-09-06
    Name: State
    Description: 角色当前行为状态。
    *****/
    public CharacterState State { get; }

    /*****
    Date: 2026-09-06
    Name: Cell
    Description: 角色当前所在格子坐标。
    *****/
    public Vector2I Cell { get; }

    /*****
    Date: 2026-09-06
    Name: CurrentTask
    Description: 角色当前正在执行的任务；空闲时为 null。
    *****/
    public ITask? CurrentTask { get; }

    /*****
    Date: 2026-09-06
    Name: WorkSpeedMultiplier
    Description: 工作效率倍率；由专长与需求综合决定。
    *****/
    public float WorkSpeedMultiplier { get; }
}
```

```csharp
namespace RelayStation.Core.Tasks;

/*****
Date: 2026-09-06
Name: TaskState
Description: 任务状态枚举；描述任务在生命周期中的当前阶段。
*****/
public enum TaskState { Pending, Assigned, InProgress, Done, Cancelled }

/*****
Date: 2026-09-06
Name: ITask
Description: 任务接口；描述一个可被角色认领并执行的工作单元。
*****/
public interface ITask
{
    /*****
    Date: 2026-09-06
    Name: State
    Description: 任务当前状态。
    *****/
    TaskState State { get; }

    /*****
    Date: 2026-09-06
    Name: Target
    Description: 任务目标设施；无目标时返回 null。
    *****/
    FacilitySim? Target { get; }

    /*****
    Date: 2026-09-06
    Name: RemainingGameMinutes
    Description: 剩余完成所需的游戏分钟数。
    *****/
    double RemainingGameMinutes { get; }

    /*****
    Date: 2026-09-06
    Name: CanAssign
    Description: 判断指定角色是否可被指派执行本任务。
    *****/
    bool CanAssign(CharacterSim c);
}

/*****
Date: 2026-09-06
Name: ITaskBoard
Description: 任务板接口；管理待处理任务并负责将任务分配给角色。
*****/
public interface ITaskBoard
{
    /*****
    Date: 2026-09-06
    Name: Pending
    Description: 当前待处理（未被认领）的任务列表。
    *****/
    IReadOnlyList<ITask> Pending { get; }

    /*****
    Date: 2026-09-06
    Name: Submit
    Description: 提交一个新任务到任务板。
    *****/
    void Submit(ITask task);

    /*****
    Date: 2026-09-06
    Name: Cancel
    Description: 取消指定任务。
    *****/
    void Cancel(ITask task);

    /*****
    Date: 2026-09-06
    Name: PickFor
    Description: 为指定角色选取一个任务；阶段一为玩家显式指派，阶段二扩展为需求+优先级自动选择。
    *****/
    ITask? PickFor(CharacterSim c);
}
```

```csharp
namespace RelayStation.Core.Facilities;

/*****
Date: 2026-09-06
Name: FacilityState
Description: 设施状态枚举；描述设施的运行状态。
*****/
public enum FacilityState { Damaged, UnderRepair, Operational }

/*****
Date: 2026-09-06
Name: FacilitySim
Description: 设施模拟对象；持有设施的定义、状态与占地信息，并提供状态变更事件。
*****/
public sealed class FacilitySim
{
    /*****
    Date: 2026-09-06
    Name: Def
    Description: 设施的静态定义（名称、占地格、修复耗时等）。
    *****/
    public FacilityDef Def { get; init; } = default!;

    /*****
    Date: 2026-09-06
    Name: State
    Description: 设施当前状态。
    *****/
    public FacilityState State { get; }

    /*****
    Date: 2026-09-06
    Name: OriginCell
    Description: 设施占地范围的原点格子坐标。
    *****/
    public Vector2I OriginCell { get; init; }

    /*****
    Date: 2026-09-06
    Name: StateChanged
    Description: 设施状态变更时触发的事件。
    *****/
    public event Action<FacilitySim, FacilityState>? StateChanged;

    // 阶段二挂点：OnOperationalTick(deltaGameMinutes) —— 供电/制氧等运行效果
}
```

```csharp
// game/Autoloads/GameRoot.cs —— 组合根

/*****
Date: 2026-09-06
Name: GameRoot
Description: 组合根与唯一 Autoload；负责装配模拟层各服务并驱动每帧的模拟推进。
*****/
public partial class GameRoot : Node
{
    /*****
    Date: 2026-09-06
    Name: Instance
    Description: GameRoot 的全局单例引用，供表现层节点访问模拟服务。
    *****/
    public static GameRoot Instance { get; private set; } = default!;

    /*****
    Date: 2026-09-06
    Name: Simulation
    Description: 模拟核心实例；持有时钟、地图、寻路、任务板、需求等服务的装配结果。
    *****/
    public Simulation Simulation { get; private set; } = default!;

    /*****
    Date: 2026-09-06
    Name: _Ready
    Description: 装配 IGameClock / IGridMap / IPathfinding / ITaskBoard / INeedSystem 等服务。
    *****/
    public override void _Ready() { }

    /*****
    Date: 2026-09-06
    Name: _Process
    Description: 每帧推进模拟：先按现实秒数推进时钟，再执行模拟 Tick。
    *****/
    public override void _Process(double delta) { Simulation.Clock.Advance(delta); Simulation.Tick(); }
}
```

## 9. 任务拆解与推进顺序

### W1：架构地基 + 格子地图

| 编号 | 任务 | 说明 | 依赖 | 验收要点 |
| --- | --- | --- | --- | --- |
| A1 | 工程骨架与规范 | 目录结构、命名空间、Autoload 注册、Main.tscn、PR 清单 | - | 空场景 F5 可运行，`main` 可编译 |
| A2 | 事件总线 + 时间系统 | `IEventBus`、`IGameClock` 及实现、单元测试 | A1 | 暂停/倍率正确；1 现实秒=1 游戏分钟有测试 |
| B1 | 格子数据模型 + 环形灰盒定稿 | `IGridMap`、方案 A/B 灰盒对比并定稿 | A1 | 五功能区数据可查询；方案结论记录在 PR |
| B2 | MapView 渲染 | `TileMapLayer` 灰盒渲染、坐标换算、hover 高亮 | B1 | 鼠标悬停正确显示格子坐标与功能区 |
| B3 | 寻路封装 | `AStarGrid2D` 封装 `IPathfinding`、路径调试可视化 | B1 | 任意两可通行格路径正确；不可达返回空 |

### W2：角色 + 设施 + 任务

| 编号 | 任务 | 说明 | 依赖 | 验收要点 |
| --- | --- | --- | --- | --- |
| C1 | 角色数据层 | `CharacterDef`×3（.tres）、`CharacterSim`、`CharacterAgent` 灰盒移动 | A2, B3 | 改配置即可 3 人同屏；角色沿路径移动 |
| C2 | 需求框架（占位，可延后） | `INeedSystem` 实现：三项需求慢速衰减、不致命 | C1 | 数值随时间变化，UI 暂不展示 |
| D1 | 设施系统 | `FacilitySim` 状态机、`FacilityView` 变色、设施 .tres | A2, B1 | 状态切换发事件且变色正确 |
| D2 | 任务系统 | `ITask` 生命周期、`TaskBoard`、`RepairTask` | C1, D1 | 认领/执行/完成/取消流转正确，有单元测试 |

### W3：行为闭环 + UI + 集成

| 编号 | 任务 | 说明 | 依赖 | 验收要点 |
| --- | --- | --- | --- | --- |
| C3 | 角色行为状态机 | Idle/Moving/Working/Interrupted 完整流转与执行 | C1, D2 | §5.3 剧本可走通 |
| E1 | 最小 UI | 时间条、设施面板+修复按钮、头顶标签 | A2, D2, C3 | 全部操作可经 UI 完成 |
| E2 | 头顶标签精修（可延后） | 标签布局与刷新时机 | E1 | 无遮挡、随状态刷新 |
| F1 | 集成 Demo 与三人验证（三人验证可延后） | Demo 场景配置、3 人同屏走查、修复闭环全流程 | 全部 | §10 DoD 逐项通过 |
| F2 | 接口冻结评审 | 对照 §8 复核签名、更新文档、标记 v1 | 全部 | 接口契约 v1 冻结 |

**范围优先级**：如需收缩范围，延后顺序为 E2 → C2 → F1 中的三人验证；其余任务为闭环必需。

**美术并行支持**：灰盒配色规范、三角色剪影占位图、设施图标线框、UI 线框图。

## 10. 阶段验收标准（DoD）

1. `Main.tscn` F5 直接运行，无报错；
2. 灰盒环形基地完整显示，5 个功能区以颜色区分，含至少 1 台「损坏」设施；
3. 点击设施 → 面板显示名称/状态 → 点「修复」→ 角色自动寻路前往 → 进度推进 → 设施变色为「运行」（§5.3 剧本）；
4. 时间条暂停/1×/2×/4× 切换正常，游戏时钟换算正确；
5. 角色头顶显示当前任务文字；
6. 角色数量由配置驱动：改资源数组即可 3 人同屏，并完成一次走查（可延后项：仅验证 1 人）；
7. `core/` 下无 Node 派生类型引用（PR 检查项）；
8. 时钟换算、任务板流转、寻路各有至少 1 个单元测试通过。

## 11. 范围外（本阶段明确不做）

- 氧气/电力/食物的真实模拟与消耗结算（仅接口与占位数值）；
- 资源库存系统、EVA、叙事者与事件、存档、声音；
- 正式美术（全灰盒）与最终 UI 布局；
- 需求致死与死亡逻辑、工作优先级面板（接口已预留）。

## 12. 为下一阶段（生存闭环）预留的挂点

| 挂点 | 用途 |
| --- | --- |
| `INeedSystem`（含三项需求） | 阶段二接入真实生存压力与工作能力影响 |
| `IGameClock.GameMinuteElapsed` | 氧气/资源模拟按游戏分钟结算 |
| `FacilitySim.OnOperationalTick`（预留签名） | 制氧机产出、供电等运行效果 |
| `TaskBoard.PickFor` 签名 | 接入工作优先级面板（需求 + 优先级 + 可用性） |
| `IResourceStore`（仅定名，不实现） | 阶段二资源库存 |

## 13. 风险与应对

| 风险 | 应对 |
| --- | --- |
| 环形格子观感不确定 | W1 初 A/B 灰盒定稿，默认 A、观感不佳降级 B，不拖延决策 |
| 接口过度设计 | 接口只覆盖本阶段 + 阶段二已知挂点（§12），更远的抽象不做 |
| 模拟/表现耦合 | `core/` 禁 Node 写入 PR 检查清单；review 严格把关 |
| 四人并行集成冲突 | A1 骨架最先合入；§8 接口草案 W1 内冻结变更需 PR；每周集成演示 |

## 14. 关联文档

- [中继站策划案_v0.1.md](../game_proposals/中继站策划案_v0.1.md)
- Godot 官方文档：TileMapLayer、AStarGrid2D、C# 全局 Autoload（https://docs.godotengine.org/）
